i = 0
#python may just remove the loop lol, k, still around 1 second, thats like 2x faster than m
#ine or 3x idk ? seems fast to me! python's? compile the asm file , it breaks tho rn cuz of gc, i can disable it so it works yes disable it
#compiled, disabled gc too cuz it breaks the loop after gc once.

for _ in range(10_000_000):
    i+=1

#Whats this? its since the last time it didnt delete itself when i fetched from git
#i


# should the lib be just a .hpp file or .hpp and .cpp